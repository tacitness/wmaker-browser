# native-host

Native messaging host source and installation templates will live here.

The host is responsible for bridging the browser extension to local
`wmaker-ng`/`ai-mcp` IPC without exposing broad filesystem or network authority.

