# extension

Browser extension source will live here.

The extension should expose semantic DOM/ARIA summaries and controls to
`wmaker-ng` through the native messaging host. It should stay small, auditable,
and compatible with Firefox ESR first.

